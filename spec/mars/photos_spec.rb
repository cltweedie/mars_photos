require 'spec_helper'

describe Mars::Photos do
  it 'has a version number' do
    expect(Mars::Photos::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
